apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: nginx-gateway-prod

resources:
  - ../../../../base/nginx-gateway/

labels:
  - pairs:
      env: production
    includeTemplates: true
    includeSelectors: false

patches:
  - target:
      kind: Namespace
      name: nginx-gateway
    patch: |-
      - op: replace
        path: /metadata/name
        value: nginx-gateway-prod

helmCharts:
  - name: nginx-gateway-fabric
    repo: oci://ghcr.io/nginx/charts/
    version: 2.5.1
    releaseName: ngf
    valuesFile: values.yaml
